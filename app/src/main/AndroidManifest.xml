<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pyojihye.translateprogram">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">

       <activity
        android:name=".Activity.IntroActivity"
        android:theme="@style/NoTitle">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
       </activity>

        <activity
            android:name=".Activity.PathActivity"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".Activity.SelectModeActivity"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".Activity.TrainingOptionActivity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="stateHidden">
        </activity>

        <activity
            android:name=".Activity.TrainingActivity"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".Activity.ViewerActivity"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".Activity.ViewerOptionActivity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="stateHidden">
        </activity>

    </application>
</manifest>